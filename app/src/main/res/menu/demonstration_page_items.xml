<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/editMode"
        android:visible="false">
        <item
            android:id="@+id/actionEdit"
            android:icon="@drawable/ic_edit_white_24dp"
            android:title="Ubah"
            app:showAsAction="ifRoom" />
    </group>

    <group android:id="@+id/viewMode">
        <item
            android:id="@+id/actionCancelParticipate"
            android:title="Batal ikut"
            android:visible="false"
            app:showAsAction="never" />
        <item
            android:id="@+id/actionCancelUpvote"
            android:title="Batal mendukung"
            android:visible="false"
            app:showAsAction="never" />
        <item
            android:id="@+id/actionCancelDownvote"
            android:title="Batal menolak"
            android:visible="false"
            app:showAsAction="never" />
    </group>
</menu>